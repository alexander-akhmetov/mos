[package]
name = "alloc_dummy"
version = "0.1.0"
authors = ["Alexander Akhmetov <me@aleks.sh>"]
edition = "2018"

[dependencies]
librust = { path = "../librust" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
